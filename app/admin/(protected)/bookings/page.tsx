import { DataTable,statusColumn } from "@/components/admin/data-table";import { adminRows } from "@/lib/admin-data";
export default async function Bookings(){const rows=await adminRows("bookings","id,reference,status,payment_status,created_at");return <><header className="admin-head"><div><p className="eyebrow">Accepted requirements</p><h1>Bookings</h1></div></header><DataTable rows={rows} columns={[{key:"reference",label:"Reference"},statusColumn,{key:"payment_status",label:"Payment",render:r=><span className="pill">{String(r.payment_status).replaceAll("_"," ")}</span>},{key:"created_at",label:"Created",render:r=>new Date(String(r.created_at)).toLocaleString()}]}/></>}
