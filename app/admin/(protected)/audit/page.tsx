import { DataTable } from "@/components/admin/data-table";import { adminRows } from "@/lib/admin-data";
export default async function Audit(){const rows=await adminRows("audit_events","id,actor_id,action,record_type,record_id,created_at");return <><header className="admin-head"><div><p className="eyebrow">Accountability</p><h1>Audit history</h1></div></header><DataTable rows={rows} columns={[{key:"action",label:"Action"},{key:"record_type",label:"Record type"},{key:"record_id",label:"Record"},{key:"actor_id",label:"Actor"},{key:"created_at",label:"Time",render:r=>new Date(String(r.created_at)).toLocaleString()}]}/></>}
