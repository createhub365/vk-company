import{notFound}from"next/navigation";import{createPrivilegedClient}from"@/lib/supabase/admin";import{SupportEditor}from"@/components/admin/support-editor";
export default async function SupportDetail({params}:{params:Promise<{id:string}>}){const{id}=await params;const db=createPrivilegedClient()!;const{data}=await db.from("support_requests").select("id,reference,name,email,phone,subject,message,shipment_reference,status,internal_notes,created_at").eq("id",id).maybeSingle();if(!data)notFound();return <><header className="admin-head"><div><p className="eyebrow">{data.reference}</p><h1>{data.subject}</h1></div></header><section className="data-card"><h2>Customer message</h2><p><strong>{data.name}</strong> · {data.email||data.phone}</p>{data.shipment_reference&&<p><strong>Shipment:</strong> {data.shipment_reference}</p>}<p>{data.message}</p></section><SupportEditor id={data.id}status={data.status}notes={data.internal_notes||""}/></>}
