"use client";import{useState}from"react";
export function ContentEditor(){const[message,setMessage]=useState("");async function save(event:React.FormEvent<HTMLFormElement>){event.preventDefault();const f=new FormData(event.currentTarget);const r=await fetch("/api/admin/content",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({key:f.get("key"),title:f.get("title"),body:f.get("body"),published:f.get("published")==="on"})});const b=await r.json();setMessage(b.ok?"Reviewed content saved.":b.message)}return <form className="data-card"onSubmit={save}><h2>Create or update by key</h2><div className="form-grid"><div className="field"><label>Content key</label><input name="key"placeholder="faq.packaging"required/></div><div className="field"><label>Title</label><input name="title"required/></div><div className="field field-full"><label>Approved body</label><textarea name="body"required/></div><label className="field-full"><input style={{width:"auto",minHeight:0}}type="checkbox"name="published"/> Mark reviewed and publish</label></div>{message&&<div className="form-status">{message}</div>}<button className="button button-small">Save content</button></form>}
