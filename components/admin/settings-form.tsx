"use client";import{useState}from"react";type Props={settings:Record<string,unknown>};
export function SettingsForm({settings}:Props){const[pending,setPending]=useState(false);const[message,setMessage]=useState("");async function submit(event:React.FormEvent<HTMLFormElement>){event.preventDefault();setPending(true);const f=new FormData(event.currentTarget);const payload:Record<string,unknown>=Object.fromEntries(f.entries());payload.pickupEnabled=f.get("pickupEnabled")==="on";const r=await fetch("/api/admin/settings",{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const b=await r.json();setMessage(b.ok?"Business settings saved.":b.message);setPending(false)}return <form className="data-card"onSubmit={submit}><h2>Published business information</h2><div className="form-grid">{[["phone","Phone"],["whatsapp","WhatsApp"],["email","Email"],["address","Address"],["mapUrl","Approved map URL"],["operatingHours","Operating hours"]].map(([key,label])=><div className={`field ${key==="address"?"field-full":""}`}key={key}><label htmlFor={key}>{label}</label><input id={key}name={key}defaultValue={String(settings[key]||"")}/></div>)}<label className="field-full"><input style={{width:"auto",minHeight:0}}type="checkbox"name="pickupEnabled"defaultChecked={Boolean(settings.pickupEnabled)}/> Enable pickup requests only after coverage and staffing are confirmed</label></div>{message&&<div className="form-status">{message}</div>}<button className="button button-small"disabled={pending}>{pending?"Saving…":"Save settings"}</button></form>}
