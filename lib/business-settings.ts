import"server-only";import{connection}from"next/server";import{businessConfig}from"@/lib/config";import{createPrivilegedClient}from"@/lib/supabase/admin";
export type PublicBusinessSettings={phone:string|null;whatsapp:string|null;email:string|null;address:string|null;mapUrl:string|null;operatingHours:string|null;pickupEnabled:boolean};
export async function getPublicBusinessSettings():Promise<PublicBusinessSettings>{await connection();const fallback={phone:businessConfig.phone,whatsapp:businessConfig.whatsapp,email:businessConfig.email,address:businessConfig.address,mapUrl:businessConfig.mapUrl,operatingHours:businessConfig.operatingHours,pickupEnabled:businessConfig.pickupRequestsEnabled};const db=createPrivilegedClient();if(!db)return fallback;const{data}=await db.from("business_settings").select("phone,whatsapp,email,address,map_url,operating_hours,pickup_enabled").eq("id",true).maybeSingle();return data?{phone:data.phone,whatsapp:data.whatsapp,email:data.email,address:data.address,mapUrl:data.map_url,operatingHours:data.operating_hours,pickupEnabled:data.pickup_enabled}:fallback;}
