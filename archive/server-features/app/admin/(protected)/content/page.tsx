import { DataTable } from "@/components/admin/data-table";import { adminRows } from "@/lib/admin-data";import{ContentEditor}from"@/components/admin/content-editor";
export default async function Content(){const rows=await adminRows("published_content","id,key,title,published,reviewed_at,updated_at");return <><header className="admin-head"><div><p className="eyebrow">Website copy</p><h1>Content</h1></div></header><div className="notice">Only reviewed and published entries can be exposed publicly. Privacy and terms remain launch blockers until approved.</div><ContentEditor/><DataTable rows={rows} columns={[{key:"key",label:"Key"},{key:"title",label:"Title"},{key:"published",label:"Published",render:r=><span className="pill">{r.published?"Yes":"Draft"}</span>},{key:"reviewed_at",label:"Reviewed"}]}/></>}
