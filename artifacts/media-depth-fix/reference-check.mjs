import {chromium} from '@playwright/test';import {writeFile} from 'node:fs/promises';
const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1440,height:900}});const r=await page.goto('https://dribbble.com/shots/27726868-AI-CFO-Platform-Landing-Page');await page.waitForTimeout(1500);console.log({status:r.status(),url:page.url(),title:await page.title(),media:await page.locator('video,video source,main img,meta[property="og:image"]').evaluateAll(es=>es.map(e=>({tag:e.tagName,src:e.currentSrc||e.src||e.content}))),text:(await page.locator('body').innerText()).slice(0,3000)});await page.screenshot({path:'artifacts/media-depth-fix/reference-access.png'});await writeFile('artifacts/media-depth-fix/reference-browser.html',await page.content());await browser.close();
