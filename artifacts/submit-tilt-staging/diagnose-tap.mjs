import{chromium}from'@playwright/test';import{writeFile}from'node:fs/promises';const browser=await chromium.launch();const page=await browser.newPage({viewport:{width:1440,height:900}});const errors=[];page.on('pageerror',e=>errors.push(e.message));
await page.addInitScript(()=>{window.events=[];for(const type of ['pointerdown','pointerup','click','scroll'])document.addEventListener(type,e=>window.events.push({type,t:performance.now(),target:e.target?.tagName,cls:e.target?.className,x:e.clientX,y:e.clientY}),{capture:true,passive:true});const append=Element.prototype.append;Element.prototype.append=function(...nodes){if(nodes.some(n=>n.className?.includes('image-feedback-layer')))window.events.push({type:'layer-created',t:performance.now()});return append.apply(this,nodes);};});
await page.goto('http://127.0.0.1:3110/services/domestic');await page.waitForTimeout(1200);const photo=page.locator('.page-hero-image');await photo.scrollIntoViewIfNeeded();await page.waitForTimeout(600);await photo.click({position:{x:40,y:40}});await page.waitForTimeout(100);console.log('layers',await page.locator('.image-feedback-layer').count(),'errors',errors);console.log(await page.evaluate(()=>window.events));await writeFile('artifacts/submit-tilt-staging/tap-diagnosis.json',JSON.stringify({errors,events:await page.evaluate(()=>window.events)},null,2));await browser.close();
