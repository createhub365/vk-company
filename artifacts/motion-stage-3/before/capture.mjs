import {chromium} from '@playwright/test';import {writeFile} from 'node:fs/promises';
const browser=await chromium.launch();const results=[];
for(const width of [1440,390]){const page=await browser.newPage({viewport:{width,height:900},deviceScaleFactor:width===390?3:2});await page.goto('http://127.0.0.1:3102');await page.locator('.cinematic-hero-background').evaluate(i=>i.decode());await page.waitForTimeout(800);await page.screenshot({path:`artifacts/motion-stage-3/before/hero-${width}.png`,scale:'css'});results.push({width,layout:await page.evaluate(()=>[...document.querySelectorAll('header,main > section,footer,.cinematic-copy h1,.cinematic-support,.cinematic-copy .lede,.cinematic-copy .hero-actions')].map(e=>{const r=e.getBoundingClientRect();return {tag:e.tagName,class:e.className,x:r.x,y:r.y+scrollY,width:r.width,height:r.height};}))});await page.close();}await browser.close();await writeFile('artifacts/motion-stage-3/before/layout.json',JSON.stringify(results,null,2));
